/**
 * Public type exports for the grid-paper npm package.
 * Consumers should import all types from this barrel:
 *   import type { DrawingDocument, Layer, VectorShape } from '@grid-paper/ui'
 */
export type {
  Point,
  LineShape,
  PolylineShape,
  CircleShape,
  RectShape,
  FreehandShape,
  VectorShape,
  ShapeType,
  GroupShape,
  LayerItem,
  Annotation,
  Layer,
  Viewport,
  GridConfig,
  DrawingDocument,
} from './canvas';
