[ ] Make sure that the app exportable to npm as a package. We want other people to be able to embed it in their apps. Excalidraw is a good example: https://github.com/excalidraw/excalidraw. Add this as a requirement to AGENTS.md
[ ] Make sure that we add to the AGENTS.md file that all drawings on the grid need to be able to defined in yaml. Make sure the yaml definition stays updated when new features are added
[ ] Add "To move canvas, hold [Scroll wheel) or|Space while dragging, or use the hand too!" to the top under the toolbar like Excalidraw. Implement these features
[ ] Define what each grid represents (cm, ft, inches, multiple inches, ...). Per default we don't assign a metric, but when we do, we can add sizes to the UI
[ ] Let users associate text with a shape?
[ ] Update the yaml spec so that we can give unique identifiers (keys) to any element. This will help us refer to the the elements and make test writing easier
[ ] Add snap to grid function. Make this the default setting

<!-- [ ] Add grid settings to yaml definition? -->

[ ] Add undo and redo buttons to left bottom corner. Also add configurable zoom percentage there

<!-- [ ] Add triangles ? -->

[ ]
